data:
  data_src: dataset.csv
  batch_size: 4
  pin_memory: true
  num_workers: 40

model:
  convolution_count: 4
  convolution_algorithm: GraphConv 
  input_dimension: 247

trainer:
  max_epochs: 30
  gpus: 1

defaults:
  - features: degree
  - logger: wandb
